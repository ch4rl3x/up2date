# docker-mqtt

Startet einen Demo-Container, einen lokalen MQTT-Broker und `up2date` im Docker-Collector-Modus.

```bash
docker compose -f examples/docker-mqtt/compose.yml up --build -d
```

Stoppen:

```bash
docker compose -f examples/docker-mqtt/compose.yml down
```
